'use strict';

const settings = {
    elements: {},
    spielfeldWidth: 500,
    spielfeldHeight: 500,
    timerID: false,
    intervalDelay: 30,
    snake: false,
    drops:[],
}

export default settings;
export let elements = settings.elements;